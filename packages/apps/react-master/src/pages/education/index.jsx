import React from 'react';
import { Outlet } from 'react-router-dom';
const Education = () => {
    return (
        <div>
            education
            <Outlet />
        </div>
    );
};

export default Education;
